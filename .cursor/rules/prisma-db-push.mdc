---
description: Always push Prisma schema changes to BOTH local and production databases
globs:
  - prisma/schema.prisma
  - src/**/*.ts
alwaysApply: true
---

# Prisma DB Push — Always Target Production

When running `prisma db push` or `prisma migrate deploy` after modifying `prisma/schema.prisma`:

## CRITICAL RULE

**ALWAYS push to BOTH databases — local AND production.**

The production DATABASE_URL is stored in your `.env.production` or Railway environment variables — never hardcode it here.

## Required Commands (in order)

```bash
# 1. Push to local
npx prisma db push --skip-generate

# 2. Push to production (NEVER forget this)
# Load DATABASE_URL from .env.production or your password manager — do NOT hardcode it here
DATABASE_URL="<from .env.production or Railway dashboard>" npx prisma db push --skip-generate

# 3. Regenerate client
npx prisma generate
```

Or use the convenience script:
```bash
npm run db:push:all
```

## Why This Matters

- The Vercel deployment uses the **production** Railway Postgres database
- If you only push to local, ALL API routes crash with `PrismaClientKnownRequestError`
- This has broken sign-in, PNS, and other features multiple times

## Never Do This

```bash
# WRONG — only updates local, production stays out of sync
npx prisma db push
```
